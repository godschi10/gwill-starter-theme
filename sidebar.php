<?php
/**
 * Sidebar Template — Stub
 *
 * This file exists to satisfy the WordPress template hierarchy and prevent
 * warnings when plugins or page builders call get_sidebar(). The theme does
 * not register any widget areas by default.
 *
 * To add widget areas:
 *   1. Register them in inc/setup.php via register_sidebar().
 *   2. Replace this stub with dynamic_sidebar() calls.
 *   3. Remove add_theme_support( 'customize-selective-refresh-widgets' )
 *      from inc/setup.php only if you also remove widget area support.
 *
 * @package GWill_Starter
 * @since   1.0.18
 */

defined( 'ABSPATH' ) || exit;
